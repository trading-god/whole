# This project resolves from the public npm registry rather than inheriting the
# machine-level registry from ~/.npmrc — its dependencies are all public, and a
# corporate mirror is only reachable on that network. Auth tokens stay in
# ~/.npmrc; nothing secret belongs in this file.
registry=https://registry.npmjs.org/
