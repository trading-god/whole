# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
.pnpm-store/

# Expo
.expo/
dist/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# OCR eval — real account screenshots are private. Only the recorded
# blocks.json + expected.json fixtures (no PII) are committed as regression data.
/packages/ocr-eval/samples/*/screenshot.*
# Derived parser trace for LLM diagnosis — regenerated from blocks.json via
# `pnpm eval:ocr:trace`, so it's a debuggable artifact, not a committed fixture.
/packages/ocr-eval/samples/*/trace.json
# LLM-generated diagnosis report — regenerated via `pnpm eval:ocr:teach`.
/packages/ocr-eval/samples/*/diagnosis.md
