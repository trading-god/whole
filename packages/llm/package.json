{
  "name": "@whole/llm",
  "version": "0.0.0",
  "private": true,
  "description": "Provider config, request construction and response parsing for a user-supplied model endpoint. Pure TypeScript with an injected fetch \u2014 no React Native, no Expo, no global fetch.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "typescript": "~6.0.3",
    "vitest": "^4.0.18"
  }
}
