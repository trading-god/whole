{
  "name": "@whole/ocr",
  "version": "0.0.0",
  "private": true,
  "description": "Account-recognition rule engine: OCR blocks of an account screenshot in, recognized accounts out. Pure TypeScript, no React Native or Expo.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^4.0.18"
  }
}
